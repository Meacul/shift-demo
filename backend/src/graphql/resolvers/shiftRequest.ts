import { getShiftRequests, getLocationById } from "../../dataSources/postgres";
import { Location, Shift, ShiftRequest, Worker } from "../../dataSources/types";

const resolvers = {
    
};

export default resolvers;
